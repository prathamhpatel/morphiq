# Google Analytics 4 measurement ID, e.g. G-XXXXXXXXXX.
# Leave unset and no analytics script is requested at all — which is what
# local builds, preview deploys and forks should do. Set it in the Vercel
# project for the Production environment only.
VITE_GA_ID=
